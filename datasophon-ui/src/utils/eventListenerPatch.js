/**
 * 全局事件监听器补丁 - 已禁用
 * 
 * 此文件已禁用所有修改，因为它干扰了编辑器功能。
 * 浏览器可能会显示一些non-passive事件监听器的警告，但这些不会影响功能。
 * 
 * 警告内容类似: [Violation] Added non-passive event listener to a scroll-blocking event
 * 这些警告只是性能建议，不是错误，可以安全忽略。
 */

// 不再应用任何补丁，只记录信息
console.log('事件监听器补丁已完全禁用，保障编辑器功能正常');

// 空导出
export default {}; 