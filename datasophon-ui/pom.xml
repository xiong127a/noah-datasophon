<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~  Licensed to the Apache Software Foundation (ASF) under one or more
  ~  contributor license agreements.  See the NOTICE file distributed with
  ~  this work for additional information regarding copyright ownership.
  ~  The ASF licenses this file to You under the Apache License, Version 2.0
  ~  (the "License"); you may not use this file except in compliance with
  ~  the License.  You may obtain a copy of the License at
  ~
  ~     http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~  Unless required by applicable law or agreed to in writing, software
  ~  distributed under the License is distributed on an "AS IS" BASIS,
  ~  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~  See the License for the specific language governing permissions and
  ~  limitations under the License.
  -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.datasophon</groupId>
        <artifactId>datasophon</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>datasophon-ui</artifactId>

    <properties>
        <!--        <build.node.version>v12.16.3</build.node.version>-->
        <!--        <build.npm.version>6.14.4</build.npm.version>-->
        <build.node.version>v18.20.8</build.node.version>
        <build.npm.version>10.8.2</build.npm.version>
        <deployed.dir>.deployed</deployed.dir>
        <dist.dir>dist</dist.dir>
        <nodemodules.dir>node_modules</nodemodules.dir>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <version>3.0.0</version>
                <configuration>
                    <!--<skip>true</skip>-->
                    <failOnError>false</failOnError>
                    <!--当配置true时,只清理filesets里的文件,构建目录中得文件不被清理.默认是flase.-->
                    <excludeDefaultDirectories>false</excludeDefaultDirectories>
                    <filesets>
                        <fileset>
                            <!--要清理的目录位置-->
                            <directory>${basedir}/dist</directory>
                            <!--是否跟随符号链接 (symbolic links)-->
                            <followSymlinks>false</followSymlinks>
                            <!--默认有些文件是不会被清理的,比如.svn文件,如果设置成false,则全部按照自定义的来处理-->
                            <useDefaultExcludes>true</useDefaultExcludes>
                            <!--对这些文件进行清理-->
                        </fileset>
                    </filesets>
                </configuration>
            </plugin>
            <!-- 禁用spring-boot-maven-plugin以避免重复构建 -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>2.5.4</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>build-image</goal>
                        </goals>
                        <phase>none</phase>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
    <profiles>
        <!-- 前端构建 profile -->
        <profile>
            <id>frontend-build</id>
            <activation>
                <!-- 使用pom 中定义的属性来控制是否激活此 profile -->
                <property>
                    <name>skip.frontend.build</name>
                    <value>false</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.github.eirslett</groupId>
                        <artifactId>frontend-maven-plugin</artifactId>
                        <version>1.10.3</version>
                        <configuration>
                            <nodeVersion>${build.node.version}</nodeVersion>
                            <npmVersion>${build.npm.version}</npmVersion>
                            <installDirectory>target</installDirectory>

                            <nodeDownloadRoot>https://nodejs.org/dist/</nodeDownloadRoot>
                            <npmDownloadRoot>https://registry.npmjs.org/npm/-/</npmDownloadRoot>
                        </configuration>
                        <executions>
                            <execution>
                                <id>install node and npm</id>
                                <goals>
                                    <goal>install-node-and-npm</goal>
                                </goals>
                                <phase>generate-resources</phase>
                            </execution>
                            <execution>
                                <id>npm install</id>
                                <goals>
                                    <goal>npm</goal>
                                </goals>
                                <phase>generate-resources</phase>
                                <configuration>
                                    <arguments>install</arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>npm build</id>
                                <goals>
                                    <goal>npm</goal>
                                </goals>
                                <phase>prepare-package</phase>
                                <configuration>
                                    <arguments>run build</arguments>
                                    <environmentVariables>
                                        <NODE_OPTIONS>--openssl-legacy-provider</NODE_OPTIONS>
                                    </environmentVariables>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>windows</id>
            <activation>
                <os>
                    <family>win</family>
                </os>
            </activation>
            <properties>
                <args.brunch>/C brunch</args.brunch>
                <args.mkdir>/C mkdir</args.mkdir>
                <args.npm>/C npm</args.npm>
                <args.rm.clean>/C rmdir /S /Q</args.rm.clean>
                <args.shell>/C</args.shell>
                <dirsep>\</dirsep>
                <envClassifier>win</envClassifier>
                <executable.brunch>cmd</executable.brunch>
                <executable.gzip>${basedir}\gzip-content.cmd</executable.gzip>
                <executable.mkdir>cmd</executable.mkdir>
                <executable.npm>cmd</executable.npm>
                <executable.rmdir>cmd</executable.rmdir>
                <executable.shell>cmd</executable.shell>
                <fileextension.shell>cmd</fileextension.shell>
                <node.executable>node.exe</node.executable>
            </properties>
        </profile>
        <profile>
            <id>linux</id>
            <activation>
                <os>
                    <family>unix</family>
                </os>
            </activation>
            <properties>
                <args.brunch/>
                <args.mkdir/>
                <args.npm/>
                <args.rm.clean>-rf</args.rm.clean>
                <args.shell/>
                <dirsep>/</dirsep>
                <envClassifier>linux</envClassifier>
                <executable.brunch>brunch</executable.brunch>
                <executable.gzip>gzip</executable.gzip>
                <executable.mkdir>mkdir</executable.mkdir>
                <executable.npm>npm</executable.npm>
                <executable.rmdir>rm</executable.rmdir>
                <executable.shell>sh</executable.shell>
                <fileextension.shell>sh</fileextension.shell>
                <node.executable>node</node.executable>
            </properties>
        </profile>
        
        <!-- Docker镜像构建 - UI -->
        <profile>
            <id>docker</id>
            <properties>
                <docker.skip>false</docker.skip>
                <docker.registry>harbor.norintech.com</docker.registry>
                <docker.host>tcp://192.168.200.6:2375</docker.host>
                <docker.tag>latest</docker.tag>
            </properties>
            <build>
                <plugins>
                    <!-- 准备 Docker 构建上下文 -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>prepare-docker-context</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/docker-context</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.basedir}</directory>
                                            <includes>
                                                <include>dist/**</include>
                                            </includes>
                                            <excludes>
                                                <exclude>dist/maven/**</exclude>
                                                <exclude>maven/**</exclude>
                                            </excludes>
                                            <targetPath>datasophon-ui</targetPath>
                                            <filtering>false</filtering>
                                        </resource>
                                        <!-- 复制nginx配置 -->
                                        <resource>
                                            <directory>${project.parent.basedir}/deploy/docker</directory>
                                            <includes>
                                                <include>nginx.conf</include>
                                            </includes>
                                            <targetPath>deploy/docker</targetPath>
                                            <filtering>false</filtering>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    
                    <!-- 使用ant插件复制Dockerfile-ui -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>copy-dockerfile</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <copy file="../Dockerfile-ui" 
                                              tofile="${project.build.directory}/docker-context/Dockerfile-ui" />
                                        <echo>Copied Dockerfile-ui to ${project.build.directory}/docker-context/</echo>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    
                    <!-- Docker 构建插件 -->
                    <plugin>
                        <groupId>io.fabric8</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <version>${docker-maven-plugin.version}</version>
                        <configuration>
                            <skip>${docker.skip}</skip>
                            <registry>${docker.registry}</registry>
                            <dockerHost>${docker.host}</dockerHost>
                            <authConfig>
                                <push>
                                    <username>noah</username>
                                    <password>Jd2019@123</password>
                                </push>
                            </authConfig>
                            <images>
                                <image>
                                    <name>${docker.registry}/bdp/datasophon-ui:${docker.tag}</name>
                                    <build>
                                        <contextDir>${project.build.directory}/docker-context</contextDir>
                                        <dockerFile>Dockerfile-ui</dockerFile>
                                        <nocache>true</nocache>
                                        <optimise>true</optimise>
                                        <cleanup>true</cleanup>
                                    </build>
                                </image>
                            </images>
                        </configuration>
                        <executions>
                            <execution>
                                <id>build-ui-image</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>build</goal>
                                    <goal>push</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
